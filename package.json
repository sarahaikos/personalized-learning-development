{
  "name": "personalized-learning-development",
  "version": "1.0.0",
  "description": "R&D Documentation Interface for Personalized Learning Research",
  "private": true,
  "scripts": {
    "predev": "node -e \"const fs = require('fs'); const path = require('path'); if (fs.existsSync('data/notes.json')) { fs.mkdirSync('public/data', { recursive: true }); fs.copyFileSync('data/notes.json', 'public/data/notes.json'); console.log('✅ Synced data/notes.json to public/data/notes.json'); }\"",
    "dev": "npm run predev && vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^4.4.0"
  }
}

